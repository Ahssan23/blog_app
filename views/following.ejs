<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BlogVibe - Following</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .fade-in {
            animation: fadeIn 0.6s ease-out forwards;
        }
        .following-item {
            transition: background-color 0.3s ease;
        }
        .following-item:hover {
            background-color: #f8fafc;
        }
        .scroll-container {
            max-height: 400px;
            overflow-y: auto;
            scrollbar-width: thin;
            scrollbar-color: #cbd5e0 #f7fafc;
        }
        .scroll-container::-webkit-scrollbar {
            width: 8px;
        }
        .scroll-container::-webkit-scrollbar-track {
            background: #f7fafc;
        }
        .scroll-container::-webkit-scrollbar-thumb {
            background: #cbd5e0;
            border-radius: 4px;
        }
    </style>
</head>
<body class="bg-stone-50 font-serif text-gray-900">
    <!-- Header -->
    <header class="bg-white border-b border-gray-200 py-6 sticky top-0 z-10">
        <div class="container mx-auto px-4 flex justify-between items-center">
            <h1 class="text-4xl font-bold text-gray-800 tracking-tight">BlogVibe</h1>
            <nav>
                <a href="http://localhost:4000" class="text-gray-600 hover:text-indigo-600 font-medium transition-colors">Home</a>
                <a href="http://localhost:4000/profile" class="text-gray-600 hover:text-indigo-600 font-medium ml-4 transition-colors">My Profile</a>
            </nav>
        </div>
    </header>

    <!-- Main Content -->
    <main class="container mx-auto px-4 py-12">
        <section class="max-w-3xl mx-auto">
            <h1 class="text-3xl font-bold text-gray-800 mb-6 fade-in">People You Follow</h1>
            <p class="text-gray-600 mb-8 text-lg fade-in" style="animation-delay: 0.1s;">Stay connected with your favorite writers!</p>
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 scroll-container">
                <% if (following.length === 0) { %>
                    <p class="text-gray-500 italic text-center">You're not following anyone yet. Discover new voices on the <a href="http://localhost:4000" class="text-indigo-600 hover:underline">home page</a>!</p>
                <% } else { %>
                    <% for(i=0; i<following.length; i++){ %>
                        <div class="following-item border-b border-gray-200 py-4 fade-in" style="animation-delay: <%= 0.2 + i * 0.1 %>s;">
                            <h2 class="text-lg font-medium text-gray-800"><%= following[i] %></h2>
                        </div>
                    <% } %>
                <% } %>
            </div>
        </section>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-900 text-gray-300 py-8">
        <div class="container mx-auto px-4 text-center">
            <p class="text-sm">© 2025 BlogVibe. Share your story with the world.</p>
            <div class="mt-4 space-x-4">
                <a href="#" class="text-gray-400 hover:text-indigo-400 text-sm transition-colors">About Us</a>
                <a href="#" class="text-gray-400 hover:text-indigo-400 text-sm transition-colors">Contact</a>
                <a href="#" class="text-gray-400 hover:text-indigo-400 text-sm transition-colors">Terms</a>
            </div>
        </div>
    </footer>
</body>
</html>